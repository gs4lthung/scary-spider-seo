# Copy to .env.local and fill in once a real domain exists. Used for
# canonical URLs, sitemap.xml, robots.txt, and Open Graph/Twitter tags
# (see lib/site.ts). Falls back to a placeholder if unset.
NEXT_PUBLIC_SITE_URL=https://scaryspiderseo.example
